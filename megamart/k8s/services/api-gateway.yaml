apiVersion: v1
kind: Service
metadata:
  name: api-gateway
  namespace: megamart
  labels:
    app: api-gateway
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: api-gateway

  ports:
    - name: http
      port: 3020
      targetPort: http