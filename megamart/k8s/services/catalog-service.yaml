apiVersion: v1
kind: Service
metadata:
  name: catalog-service
  namespace: megamart
  labels:
    app: catalog-service
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: catalog-service

  ports:
    - name: http
      port: 3004
      targetPort: http