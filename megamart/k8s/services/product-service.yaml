apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: megamart
  labels:
    app: product-service
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: product-service

  ports:
    - name: http
      port: 3003
      targetPort: http