apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: megamart
  labels:
    app: user-service
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: user-service

  ports:
    - name: http
      port: 3002
      targetPort: http