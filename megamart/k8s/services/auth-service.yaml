apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: megamart
  labels:
    app: auth-service
    monitoring: prometheus

spec:
  type: ClusterIP

  selector:
    app: auth-service

  ports:
    - name: http
      port: 3001
      targetPort: http