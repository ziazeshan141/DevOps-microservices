apiVersion: v1
kind: Service
metadata:
  name: review-service
  namespace: megamart
  labels:
    app: review-service
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: review-service

  ports:
    - name: http
      port: 3011
      targetPort: http