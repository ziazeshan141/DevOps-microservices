apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: megamart
  labels:
    app: shipping-service
    monitoring: prometheus

spec:
  type: ClusterIP
  selector:
    app: shipping-service

  ports:
    - name: http
      port: 3009
      targetPort: http