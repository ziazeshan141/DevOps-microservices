apiVersion: apps/v1
kind: Deployment
metadata:
  name: fraud-service
  namespace: megamart

spec:
  replicas: 1

  selector:
    matchLabels:
      app: fraud-service

  template:
    metadata:
      labels:
        app: fraud-service

    spec:
      containers:
        - name: fraud-service
          image: 047385030300.dkr.ecr.us-east-1.amazonaws.com/fraud-service:latest

          ports:
            - name: http
              containerPort: 3024

          envFrom:
            - configMapRef:
                name: megamart-config
            - secretRef:
                name: megamart-secrets

          env:
            - name: PORT
              value: "3024"

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_PASSWORD

            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/fraud_db"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
