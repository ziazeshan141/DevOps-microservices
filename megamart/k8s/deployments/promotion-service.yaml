apiVersion: apps/v1
kind: Deployment
metadata:
  name: promotion-service
  namespace: megamart

spec:
  replicas: 1

  selector:
    matchLabels:
      app: promotion-service

  template:
    metadata:
      labels:
        app: promotion-service

    spec:
      containers:
        - name: promotion-service
          image: AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/promotion-service:latest

          ports:
            - name: http
              containerPort: 3016

          envFrom:
            - configMapRef:
                name: megamart-config
            - secretRef:
                name: megamart-secrets

          env:
            - name: PORT
              value: "3016"

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_PASSWORD

            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/promotion_db"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10