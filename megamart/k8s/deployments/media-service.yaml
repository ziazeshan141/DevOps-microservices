apiVersion: apps/v1
kind: Deployment
metadata:
  name: media-service
  namespace: megamart

spec:
  replicas: 1

  selector:
    matchLabels:
      app: media-service

  template:
    metadata:
      labels:
        app: media-service

    spec:
      containers:
        - name: media-service
          image: AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/media-service:latest

          ports:
            - name: http
              containerPort: 3022

          envFrom:
            - configMapRef:
                name: megamart-config
            - secretRef:
                name: megamart-secrets

          env:
            - name: PORT
              value: "3022"

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_PASSWORD

            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/media_db"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10