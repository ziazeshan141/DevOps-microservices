apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipping-service
  namespace: megamart

spec:
  replicas: 1

  selector:
    matchLabels:
      app: shipping-service

  template:
    metadata:
      labels:
        app: shipping-service

    spec:
      containers:
        - name: shipping-service
          image: AWS_ACCOUNT_ID.dkr.ecr.us-east-1.amazonaws.com/shipping-service:latest

          ports:
            - name: http
              containerPort: 3009

          envFrom:
            - configMapRef:
                name: megamart-config
            - secretRef:
                name: megamart-secrets

          env:
            - name: PORT
              value: "3009"

            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_USER

            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: POSTGRES_PASSWORD

            - name: DATABASE_URL
              value: "postgres://$(POSTGRES_USER):$(POSTGRES_PASSWORD)@postgres:5432/shipping_db"

            - name: RABBITMQ_USER
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: RABBITMQ_USER

            - name: RABBITMQ_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: megamart-secrets
                  key: RABBITMQ_PASSWORD

            - name: RABBITMQ_URL
              value: "amqp://$(RABBITMQ_USER):$(RABBITMQ_PASSWORD)@rabbitmq:5672"

            - name: EVENT_EXCHANGE
              value: "megamart.events"

          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10