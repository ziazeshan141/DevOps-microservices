const r=require('express').Router(),c=require('./controllers/controller'),a=require('./middleware/authMiddleware'); r.get('/api/v1/products',c.list);r.get('/api/v1/products/:id',c.get);r.post('/api/v1/products',a.requireAuth,a.requireRole('admin'),c.create);r.put('/api/v1/products/:id',a.requireAuth,a.requireRole('admin'),c.update);r.delete('/api/v1/products/:id',a.requireAuth,a.requireRole('admin'),c.remove);module.exports=r;