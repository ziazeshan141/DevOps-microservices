const {publishEvent}=require('../lib/events');async function send({userId,channel='email',template='generic',data={}}){const notification={id:crypto.randomUUID(),userId,channel,template,data,status:'sent',sentAt:new Date().toISOString()};console.log('NOTIFICATION',JSON.stringify(notification));await publishEvent('notification.sent',notification);return notification}const crypto=require('crypto');module.exports={send};