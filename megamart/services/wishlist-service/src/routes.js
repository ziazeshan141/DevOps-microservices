const r=require('express').Router(),a=require('./middleware/authMiddleware'),{query}=require('./config/database');r.use('/api/v1/wishlist',a.requireAuth);r.get('/api/v1/wishlist',async(req,res,next)=>{try{res.json((await query('SELECT * FROM wishlist_items WHERE user_id=$1 ORDER BY created_at DESC',[req.user.sub])).rows)}catch(e){next(e)}});r.post('/api/v1/wishlist/:productId',async(req,res,next)=>{try{res.status(201).json((await query('INSERT INTO wishlist_items(user_id,product_id) VALUES($1,$2) ON CONFLICT(user_id,product_id) DO UPDATE SET updated_at=now() RETURNING *',[req.user.sub,req.params.productId])).rows[0])}catch(e){next(e)}});r.delete('/api/v1/wishlist/:productId',async(req,res,next)=>{try{await query('DELETE FROM wishlist_items WHERE user_id=$1 AND product_id=$2',[req.user.sub,req.params.productId]);res.status(204).end()}catch(e){next(e)}});module.exports=r;