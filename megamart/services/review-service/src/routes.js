const r=require('express').Router(),a=require('./middleware/authMiddleware'),{query}=require('./config/database');r.get('/api/v1/reviews/product/:productId',async(req,res,next)=>{try{res.json((await query(`SELECT * FROM reviews WHERE product_id=$1 AND status='published' ORDER BY created_at DESC`,[req.params.productId])).rows)}catch(e){next(e)}});r.post('/api/v1/reviews',a.requireAuth,async(req,res,next)=>{try{const b=req.body;const x=(await query(`INSERT INTO reviews(product_id,user_id,rating,title,body) VALUES($1,$2,$3,$4,$5) ON CONFLICT(product_id,user_id) DO UPDATE SET rating=excluded.rating,title=excluded.title,body=excluded.body,updated_at=now() RETURNING *`,[b.productId,req.user.sub,b.rating,b.title,b.body])).rows[0];res.status(201).json(x)}catch(e){next(e)}});module.exports=r;