const {query}=require('../config/database');const s=require('../services/service');async function get(req,res,next){try{const x=(await query(`SELECT *,CASE WHEN sale_price IS NOT NULL AND (starts_at IS NULL OR starts_at<=now()) AND (ends_at IS NULL OR ends_at>=now()) THEN sale_price ELSE base_price END AS effective_price FROM prices WHERE product_id=$1 AND active=true ORDER BY created_at DESC LIMIT 1`,[req.params.productId])).rows[0];if(!x)return res.status(404).json({error:'PRICE_NOT_FOUND'});res.json(x)}catch(e){next(e)}}async function create(req,res,next){try{res.status(201).json(await s.create(req.body))}catch(e){next(e)}}async function update(req,res,next){try{res.json(await s.update(req.params.id,req.body))}catch(e){next(e)}}module.exports={get,create,update};