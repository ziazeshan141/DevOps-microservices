const r=require('express').Router(),a=require('./middleware/authMiddleware'),{query}=require('./config/database');r.get('/api/v1/ratings/:productId',async(req,res,next)=>{try{const x=(await query('SELECT product_id,COUNT(*)::int AS count,COALESCE(AVG(rating),0)::numeric(3,2) AS average FROM ratings WHERE product_id=$1 GROUP BY product_id',[req.params.productId])).rows[0]||{product_id:req.params.productId,count:0,average:0};res.json(x)}catch(e){next(e)}});r.put('/api/v1/ratings/:productId',a.requireAuth,async(req,res,next)=>{try{const x=(await query(`INSERT INTO ratings(product_id,user_id,rating) VALUES($1,$2,$3) ON CONFLICT(product_id,user_id) DO UPDATE SET rating=excluded.rating,updated_at=now() RETURNING *`,[req.params.productId,req.user.sub,req.body.rating])).rows[0];res.json(x)}catch(e){next(e)}});module.exports=r;