const {requestJson}=require('../lib/http');async function recommend(limit=10){const products=await requestJson(`${process.env.PRODUCT_SERVICE_URL||'http://localhost:3003'}/api/v1/products?limit=50`);const scored=await Promise.all(products.map(async p=>{let rating={average:0,count:0};try{rating=await requestJson(`${process.env.RATING_SERVICE_URL||'http://localhost:3012'}/api/v1/ratings/${p.id}`)}catch(_){}return{...p,recommendationScore:Number(rating.average||0)*Math.log2(Number(rating.count||0)+2)}}));return scored.sort((a,b)=>b.recommendationScore-a.recommendationScore).slice(0,Math.min(Number(limit)||10,50))}module.exports={recommend};