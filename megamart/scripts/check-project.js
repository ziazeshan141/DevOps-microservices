const fs=require('fs'),path=require('path'),{spawnSync}=require('child_process');const root=path.join(__dirname,'..','services');let failures=0;for(const name of fs.readdirSync(root)){const src=path.join(root,name,'src');for(const file of walk(src)){if(file.endsWith('.js')){const r=spawnSync(process.execPath,['--check',file],{encoding:'utf8'});if(r.status!==0){console.error(name,file,r.stderr);failures++}}}}function walk(dir){return fs.readdirSync(dir,{withFileTypes:true}).flatMap(e=>e.isDirectory()?walk(path.join(dir,e.name)):[path.join(dir,e.name)])}if(failures)process.exit(1);console.log('Syntax check passed for all service JavaScript files.');